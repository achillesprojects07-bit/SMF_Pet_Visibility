#!/usr/bin/env node

/**
 * SOURCE MASTER SYNC — production-safe, source-file driven.
 *
 * Supported business operations ONLY:
 *   1) RENAME an existing store from STORE IDENTITY & SCHEDULE.
 *   2) ADD one new store from a complete blank-ID / blank-Legacy-Key source row.
 *
 * Safety rules:
 * - Source workbook is authoritative.
 * - Existing stores are matched by permanent Store ID only.
 * - Existing Legacy Store Key is immutable and never regenerated on rename.
 * - V4_POE and V4_PHOTOS are never written.
 * - Existing Drive files/folders are never renamed, moved or deleted.
 * - No DELETE path exists.
 * - PLAN is the default and is read-only.
 * - APPLY requires exact target + exact current fingerprint + explicit approval.
 * - ADD is one row at a time and contains partial-transaction recovery.
 */

import fs from 'node:fs';
import crypto from 'node:crypto';

const SOURCE_SHEET_ID=process.env.SOURCE_SHEET_ID||'1_qtlUdaytLA_zR03rqBh8RYP-MWjYKVaRoABZvYFBlg';
const INTERNAL_SHEET_ID=process.env.GOOGLE_SHEET_ID||'1TNrb3ir8vS6CyZ8kXhRH43JXoH4Ws7iQ4g3649KQTf0';
const POE_ROOT_FOLDER_ID=process.env.POE_ROOT_FOLDER_ID||'1sD4bBdsaEqxEOfk-Q9yJIXyS_ErrLmAw';
const IDENTITY_SHEET='STORE IDENTITY & SCHEDULE';
const HISTORY_SHEET='STORE CHANGE HISTORY';
const MATERIAL_SHEET='MATERIAL SET DEFINITIONS';
const OUT_DIR=process.env.SOURCE_MASTER_SYNC_OUT_DIR||'migration/source-master-sync-output';
const MODE=String(process.env.SOURCE_MASTER_SYNC_MODE||'PLAN').toUpperCase();
const TARGET=String(process.env.SOURCE_MASTER_SYNC_TARGET||'').trim();
const EXPECTED_FINGERPRINT=String(process.env.SOURCE_MASTER_SYNC_EXPECTED_FINGERPRINT||'').trim();
const APPROVAL=String(process.env.SOURCE_MASTER_SYNC_APPROVAL||'').trim();
const APPROVAL_PHRASE='YES_I_APPROVE_SOURCE_MASTER_SYNC';

const V4_HEADERS=['Store Key','Store Name','Assigned Team','Day','Stop No.','Store Category','Address','Barangay','Area','Material Allocation','Material JSON','Pending','Category Rule','Override Rule','Effective Rule','Active','Source Sync Timestamp','Store ID'];
const SOURCE_HEADERS=['Store ID','Current Store Name','Previous Name','Assigned Team','Route Day','Route Stop','Scheduled Deployment Date','Scheduled Stop','Actual Visit Date','Store Category','Street Address / Location','Barangay / District','City / Area','Material Allocation','Active','Legacy Store Key','Identity Status','Last Identity Sync'];
const NEW_REQUIRED=['Current Store Name','Assigned Team','Route Day','Route Stop','Scheduled Deployment Date','Scheduled Stop','Store Category','Street Address / Location','City / Area','Material Allocation','Active'];

function text(v){return String(v??'').trim();}
function norm(v){return text(v).normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ');}
function hash(v){return crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex');}
function teamNorm(v){const m=text(v).match(/team\s*(\d+)/i);return m?`Team ${m[1]}`:text(v);}
function boolNorm(v){const s=text(v).toLowerCase();return ['true','yes','1','active'].includes(s)?'TRUE':['false','no','0','inactive'].includes(s)?'FALSE':text(v).toUpperCase();}
function rows(values){if(!values?.length)return[];const h=values[0].map(text);return values.slice(1).filter(r=>r.some(v=>text(v)!=='')).map((r,i)=>{const o={__row:i+2};h.forEach((k,j)=>{if(k)o[k]=r[j]??''});return o;});}
function indexMap(headers){const m={};headers.forEach((h,i)=>m[h]=i);return m;}
function isoNow(){return new Date().toISOString();}
function a1Col(n){let s='';for(let x=n+1;x>0;x=Math.floor((x-1)/26))s=String.fromCharCode(65+((x-1)%26))+s;return s;}
function dateDisplay(v){const s=text(v);let m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/);if(m){const names=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sept','Oct','Nov','Dec'];return `${names[Number(m[2])-1]} ${Number(m[3])}`;}m=s.match(/Sept(?:ember)?\s+(\d{1,2})/i);return m?`Sept ${Number(m[1])}`:s;}
function duplicates(items){const seen=new Set(),dup=new Set();for(const x of items.map(text).filter(Boolean)){if(seen.has(x))dup.add(x);seen.add(x)}return [...dup];}
function legacyKey(row){return `${teamNorm(row['Assigned Team'])}|${dateDisplay(row['Scheduled Deployment Date'])}|${text(row['Scheduled Stop'])}|${text(row['Current Store Name'])}`;}
function nextId(live,source){let max=0;for(const r of [...live,...source]){const m=text(r['Store ID']).match(/^SMF-(\d{4})$/);if(m)max=Math.max(max,Number(m[1]));}return `SMF-${String(max+1).padStart(4,'0')}`;}

async function token(){const need=n=>{const v=text(process.env[n]);if(!v)throw new Error(`Missing ${n}`);return v;};const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({client_id:need('GOOGLE_OAUTH_CLIENT_ID'),client_secret:need('GOOGLE_OAUTH_CLIENT_SECRET'),refresh_token:need('GOOGLE_OAUTH_REFRESH_TOKEN'),grant_type:'refresh_token'})});const j=await r.json();if(!r.ok||!j.access_token)throw new Error(`OAuth refresh failed: ${r.status}`);return j.access_token;}
async function getRange(tok,id,range){const u=`https://sheets.googleapis.com/v4/spreadsheets/${encodeURIComponent(id)}/values/${encodeURIComponent(range)}?majorDimension=ROWS&valueRenderOption=FORMATTED_VALUE`;const r=await fetch(u,{headers:{authorization:`Bearer ${tok}`}});if(!r.ok)throw new Error(`GET ${range} failed: ${r.status} ${await r.text()}`);return(await r.json()).values||[];}
async function putRange(tok,id,range,values){const u=`https://sheets.googleapis.com/v4/spreadsheets/${encodeURIComponent(id)}/values/${encodeURIComponent(range)}?valueInputOption=RAW`;const r=await fetch(u,{method:'PUT',headers:{authorization:`Bearer ${tok}`,'content-type':'application/json'},body:JSON.stringify({values})});if(!r.ok)throw new Error(`WRITE ${range} failed: ${r.status} ${await r.text()}`);return r.json();}
async function appendRange(tok,id,range,values){const u=`https://sheets.googleapis.com/v4/spreadsheets/${encodeURIComponent(id)}/values/${encodeURIComponent(range)}:append?valueInputOption=RAW&insertDataOption=INSERT_ROWS`;const r=await fetch(u,{method:'POST',headers:{authorization:`Bearer ${tok}`,'content-type':'application/json'},body:JSON.stringify({values})});if(!r.ok)throw new Error(`APPEND ${range} failed: ${r.status} ${await r.text()}`);return r.json();}
async function listFolders(tok,parent){const q=`'${String(parent).replace(/'/g,"\\'")}' in parents and mimeType='application/vnd.google-apps.folder' and trashed=false`;const u=`https://www.googleapis.com/drive/v3/files?spaces=drive&pageSize=1000&fields=files(id,name)&q=${encodeURIComponent(q)}`;const r=await fetch(u,{headers:{authorization:`Bearer ${tok}`}});if(!r.ok)throw new Error(`Drive list failed: ${r.status}`);return(await r.json()).files||[];}
async function getOrCreateFolder(tok,parent,name){const existing=(await listFolders(tok,parent)).find(f=>norm(f.name)===norm(name));if(existing)return{id:existing.id,name:existing.name,created:false};const r=await fetch('https://www.googleapis.com/drive/v3/files?fields=id,name',{method:'POST',headers:{authorization:`Bearer ${tok}`,'content-type':'application/json'},body:JSON.stringify({name,mimeType:'application/vnd.google-apps.folder',parents:[parent]})});if(!r.ok)throw new Error(`Drive folder create failed: ${r.status} ${await r.text()}`);const j=await r.json();return{id:j.id,name:j.name,created:true};}

function materialDefinitions(values){const headerRow=values.findIndex(r=>text(r[0]).toUpperCase()==='SET');if(headerRow<0)throw new Error('MATERIAL SET DEFINITIONS header row not found.');const h=values[headerRow].map(text),out=new Map();for(const r of values.slice(headerRow+1)){const set=text(r[0]).toUpperCase();if(!set)continue;const o={};h.forEach((k,i)=>o[k]=r[i]??'');out.set(set,o);}return out;}
function materialJson(setName,defs){const set=text(setName).toUpperCase(),d=defs.get(set);if(!d)throw new Error(`Unknown Material Allocation: ${setName}`);if(set==='PENDING'||set==='UNIQUE')throw new Error(`${set} requires explicit reviewed quantities and cannot be auto-added.`);const map=[['Flyers','Flyers'],['Posters','Posters'],['Price Puppy','Price Puppy'],['Price Adult','Price Adult'],['Price Small Breed','Price Small Breed'],['Price Generic','Price Generic'],['Available Here','Available Here'],['Banner','Horizontal Banner'],['Shirt','Shirt'],['Wobbler','Wobbler'],['Shelf Strip','Shelf Strip']];const obj={};for(const[src,dst]of map){const n=Number(d[src]||0);if(Number.isFinite(n)&&n!==0)obj[dst]=n;}if(!Object.keys(obj).length)throw new Error('Material allocation resolved to an empty quantity set.');return obj;}
function ruleForSet(set){const s=text(set).toUpperCase();if(s==='SET 1')return'FEED';if(s==='SET 2')return'PET_SHOP_MT';if(s==='SET 3')return'VET';if(['SET 4','SET 5','SET 6'].includes(s))return'MODERN_TRADE';throw new Error(`No auto-add category rule mapping for ${set}`);}

function sameAddIdentity(sourceRow,liveRow){return norm(sourceRow['Current Store Name'])===norm(liveRow['Store Name'])&&teamNorm(sourceRow['Assigned Team'])===teamNorm(liveRow['Assigned Team'])&&dateDisplay(sourceRow['Scheduled Deployment Date'])===dateDisplay(liveRow.Day)&&text(sourceRow['Scheduled Stop'])===text(liveRow['Stop No.'])&&norm(sourceRow['Street Address / Location'])===norm(liveRow.Address)&&norm(sourceRow['City / Area'])===norm(liveRow.Area);}

function plan(source,live,defs){
  const liveById=new Map(live.map(r=>[text(r['Store ID']),r])),actions=[],hardBlocks=[];
  const sourceIds=source.map(r=>text(r['Store ID'])),liveIds=live.map(r=>text(r['Store ID']));
  if(duplicates(sourceIds).length)hardBlocks.push({reason:'DUPLICATE_SOURCE_STORE_ID',values:duplicates(sourceIds)});
  if(duplicates(liveIds).length)hardBlocks.push({reason:'DUPLICATE_LIVE_STORE_ID',values:duplicates(liveIds)});
  if(duplicates(live.map(r=>r['Store Key'])).length)hardBlocks.push({reason:'DUPLICATE_LIVE_STORE_KEY',values:duplicates(live.map(r=>r['Store Key']))});

  for(const s of source){const id=text(s['Store ID']),key=text(s['Legacy Store Key']);
    if(id){const l=liveById.get(id);if(!l){actions.push({action:'BLOCKED',reason:'SOURCE_ID_NOT_IN_LIVE',storeId:id,sourceRow:s.__row});continue;}if(key!==text(l['Store Key'])){actions.push({action:'BLOCKED',reason:'LEGACY_KEY_MISMATCH',storeId:id,sourceRow:s.__row});continue;}
      const diffs=[];if(norm(s['Current Store Name'])!==norm(l['Store Name']))diffs.push({field:'Current Store Name',current:text(l['Store Name']),desired:text(s['Current Store Name']),allowed:true});
      const forbidden=[['Assigned Team','Assigned Team','team'],['Scheduled Deployment Date','Day','date'],['Scheduled Stop','Stop No.','text'],['Store Category','Store Category','norm'],['Street Address / Location','Address','norm'],['Barangay / District','Barangay','norm'],['City / Area','Area','norm'],['Material Allocation','Material Allocation','norm'],['Active','Active','bool']];
      for(const[sf,lf,kind]of forbidden){let a=s[sf],b=l[lf];if(kind==='team'){a=teamNorm(a);b=teamNorm(b)}else if(kind==='date'){a=dateDisplay(a);b=dateDisplay(b)}else if(kind==='norm'){a=norm(a);b=norm(b)}else if(kind==='bool'){a=boolNorm(a);b=boolNorm(b)}else{a=text(a);b=text(b)}if(a!==b)diffs.push({field:sf,current:text(l[lf]),desired:text(s[sf]),allowed:false});}
      if(diffs.some(d=>!d.allowed))actions.push({action:'BLOCKED',reason:'ONLY_STORE_NAME_CHANGE_IS_ENABLED',storeId:id,sourceRow:s.__row,changes:diffs});else if(diffs.length)actions.push({action:'RENAME_READY',storeId:id,sourceRow:s.__row,legacyStoreKey:key,oldName:text(l['Store Name']),newName:text(s['Current Store Name'])});else actions.push({action:'UNCHANGED',storeId:id,sourceRow:s.__row});
    }else{
      const missing=NEW_REQUIRED.filter(f=>!text(s[f]));if(key){actions.push({action:'BLOCKED',reason:'NEW_ROW_LEGACY_KEY_MUST_BE_BLANK',sourceRow:s.__row});continue;}if(missing.length){actions.push({action:'BLOCKED',reason:'NEW_ROW_MISSING_REQUIRED_FIELDS',sourceRow:s.__row,missing});continue;}if(boolNorm(s.Active)!=='TRUE'){actions.push({action:'BLOCKED',reason:'NEW_ROW_ACTIVE_MUST_BE_TRUE',sourceRow:s.__row});continue;}
      let mat,rule;try{mat=materialJson(s['Material Allocation'],defs);rule=ruleForSet(s['Material Allocation']);}catch(e){actions.push({action:'BLOCKED',reason:'NEW_ROW_MATERIAL_INVALID',sourceRow:s.__row,detail:e.message});continue;}
      const deterministicKey=legacyKey(s);const recovery=live.find(l=>text(l['Store Key'])===deterministicKey&&sameAddIdentity(s,l));if(recovery){actions.push({action:'ADD_RECOVER_READY',sourceRow:s.__row,storeId:text(recovery['Store ID']),legacyStoreKey:deterministicKey,currentName:text(s['Current Store Name'])});continue;}
      const nameRisk=live.filter(l=>norm(l['Store Name'])===norm(s['Current Store Name']));const addressRisk=live.filter(l=>norm(l.Address)===norm(s['Street Address / Location'])&&teamNorm(l['Assigned Team'])===teamNorm(s['Assigned Team']));if(nameRisk.length||addressRisk.length){actions.push({action:'BLOCKED',reason:'POSSIBLE_DUPLICATE_NEW_STORE',sourceRow:s.__row,duplicateStoreIds:[...new Set([...nameRisk,...addressRisk].map(x=>text(x['Store ID'])))]});continue;}
      actions.push({action:'ADD_READY',sourceRow:s.__row,proposedStoreId:nextId(live,source),proposedLegacyStoreKey:deterministicKey,currentName:text(s['Current Store Name']),team:teamNorm(s['Assigned Team']),area:text(s['City / Area']),materialJson:mat,rule});
    }
  }
  const actionable=actions.filter(a=>['RENAME_READY','ADD_READY','ADD_RECOVER_READY'].includes(a.action));const fingerprint=hash({source:source.map(r=>({...r,__row:r.__row})),live:live.map(r=>({...r,__row:r.__row})),actions:actionable,hardBlocks});return{hardBlocks,actions,actionable,fingerprint};
}

function requireApply(p,action){if(APPROVAL!==APPROVAL_PHRASE)throw new Error(`APPLY blocked: approval phrase must equal ${APPROVAL_PHRASE}`);if(!EXPECTED_FINGERPRINT||EXPECTED_FINGERPRINT!==p.fingerprint)throw new Error('APPLY blocked: fingerprint mismatch. Re-run PLAN.');if(p.hardBlocks.length)throw new Error('APPLY blocked: global integrity block exists.');if(!TARGET)throw new Error('APPLY blocked: exact target is required.');if(action==='RENAME'){const a=p.actions.find(x=>x.action==='RENAME_READY'&&x.storeId===TARGET);if(!a)throw new Error(`No RENAME_READY action for ${TARGET}`);return a;}if(action==='ADD'){const row=Number(TARGET),a=p.actions.find(x=>['ADD_READY','ADD_RECOVER_READY'].includes(x.action)&&Number(x.sourceRow)===row);if(!a)throw new Error(`No ADD_READY/ADD_RECOVER_READY action for source row ${TARGET}`);return a;}throw new Error('Unsupported apply action.');}

async function writeSourceIdentity(tok,sourceValues,sourceRow,storeId,key,status='ACTIVE'){const si=indexMap(sourceValues[0].map(text)),now=isoNow();for(const[field,value]of[['Store ID',storeId],['Legacy Store Key',key],['Identity Status',status],['Last Identity Sync',now]]){if(si[field]===undefined)throw new Error(`Source column missing: ${field}`);await putRange(tok,SOURCE_SHEET_ID,`'${IDENTITY_SHEET}'!${a1Col(si[field])}${sourceRow}:${a1Col(si[field])}${sourceRow}`,[[value]]);}return now;}

async function applyRename(tok,a,sourceValues,liveValues){const li=indexMap(liveValues[0].map(text)),si=indexMap(sourceValues[0].map(text)),liveRow=rows(liveValues).find(r=>text(r['Store ID'])===a.storeId);if(!liveRow)throw new Error('Target live store disappeared.');if(text(liveRow['Store Key'])!==a.legacyStoreKey)throw new Error('Legacy Store Key changed during apply.');const now=isoNow(),lr=liveRow.__row;await putRange(tok,INTERNAL_SHEET_ID,`V4_STORES!${a1Col(li['Store Name'])}${lr}:${a1Col(li['Store Name'])}${lr}`,[[a.newName]]);if(li['Source Sync Timestamp']!==undefined)await putRange(tok,INTERNAL_SHEET_ID,`V4_STORES!${a1Col(li['Source Sync Timestamp'])}${lr}:${a1Col(li['Source Sync Timestamp'])}${lr}`,[[now]]);for(const[field,value]of[['Previous Name',a.oldName],['Identity Status','ACTIVE_WITH_HISTORY'],['Last Identity Sync',now]])if(si[field]!==undefined)await putRange(tok,SOURCE_SHEET_ID,`'${IDENTITY_SHEET}'!${a1Col(si[field])}${a.sourceRow}:${a1Col(si[field])}${a.sourceRow}`,[[value]]);await appendRange(tok,SOURCE_SHEET_ID,`'${HISTORY_SHEET}'!A:J`,[[`EVT-${Date.now()}-${a.storeId}`,a.storeId,a.legacyStoreKey,'STORE_RENAME','Current Store Name',a.oldName,a.newName,now,'SOURCE_MASTER_SYNC','SYSTEM']]);const vr=rows(await getRange(tok,INTERNAL_SHEET_ID,'V4_STORES!A1:S1000')).find(r=>text(r['Store ID'])===a.storeId);if(!vr||text(vr['Store Name'])!==a.newName||text(vr['Store Key'])!==a.legacyStoreKey)throw new Error('Post-rename verification failed.');return{ok:true,operation:'RENAME',storeId:a.storeId,oldName:a.oldName,newName:a.newName,legacyStoreKeyPreserved:true};}

async function applyAdd(tok,a,sourceValues,liveValues,defs){const sourceRow=rows(sourceValues).find(r=>Number(r.__row)===Number(a.sourceRow));if(!sourceRow)throw new Error('Source row disappeared.');if(text(sourceRow['Store ID'])||text(sourceRow['Legacy Store Key']))throw new Error('Source row no longer blank-ID/blank-key. Re-plan.');if(a.action==='ADD_RECOVER_READY'){await writeSourceIdentity(tok,sourceValues,a.sourceRow,a.storeId,a.legacyStoreKey,'ACTIVE');await appendRange(tok,SOURCE_SHEET_ID,`'${HISTORY_SHEET}'!A:J`,[[`EVT-${Date.now()}-${a.storeId}`,a.storeId,a.legacyStoreKey,'STORE_ADD_RECOVERY','Store ID','',a.storeId,isoNow(),'SOURCE_MASTER_SYNC','SYSTEM']]);return{ok:true,operation:'ADD_RECOVERY',storeId:a.storeId,legacyStoreKey:a.legacyStoreKey};}
  const liveRows=rows(liveValues);if(liveRows.some(r=>text(r['Store ID'])===a.proposedStoreId||text(r['Store Key'])===a.proposedLegacyStoreKey))throw new Error('Proposed Store ID or Legacy Store Key already exists. Re-plan.');const now=isoNow(),mat=materialJson(sourceRow['Material Allocation'],defs),rule=ruleForSet(sourceRow['Material Allocation']);const liveFolder=await getOrCreateFolder(tok,POE_ROOT_FOLDER_ID,'LIVE'),teamFolder=await getOrCreateFolder(tok,liveFolder.id,teamNorm(sourceRow['Assigned Team'])),areaFolder=await getOrCreateFolder(tok,teamFolder.id,text(sourceRow['City / Area'])),storeFolder=await getOrCreateFolder(tok,areaFolder.id,`${a.proposedStoreId} - ${text(sourceRow['Current Store Name'])}`);if(!storeFolder.id)throw new Error('Store POE folder provisioning failed.');const record={'Store Key':a.proposedLegacyStoreKey,'Store Name':text(sourceRow['Current Store Name']),'Assigned Team':teamNorm(sourceRow['Assigned Team']),'Day':dateDisplay(sourceRow['Scheduled Deployment Date']),'Stop No.':text(sourceRow['Scheduled Stop']),'Store Category':text(sourceRow['Store Category']),'Address':text(sourceRow['Street Address / Location']),'Barangay':text(sourceRow['Barangay / District']),'Area':text(sourceRow['City / Area']),'Material Allocation':text(sourceRow['Material Allocation']),'Material JSON':JSON.stringify(mat),'Pending':'FALSE','Category Rule':rule,'Override Rule':'','Effective Rule':rule,'Active':'TRUE','Source Sync Timestamp':now,'Store ID':a.proposedStoreId};await appendRange(tok,INTERNAL_SHEET_ID,'V4_STORES!A:R',[[...V4_HEADERS.map(h=>record[h]??'')]]);const verifyBeforeSource=rows(await getRange(tok,INTERNAL_SHEET_ID,'V4_STORES!A1:S1000')).filter(r=>text(r['Store ID'])===a.proposedStoreId);if(verifyBeforeSource.length!==1||text(verifyBeforeSource[0]['Store Key'])!==a.proposedLegacyStoreKey)throw new Error('Post-live-add verification failed before source identity write.');await writeSourceIdentity(tok,sourceValues,a.sourceRow,a.proposedStoreId,a.proposedLegacyStoreKey,'ACTIVE');await appendRange(tok,SOURCE_SHEET_ID,`'${HISTORY_SHEET}'!A:J`,[[`EVT-${Date.now()}-${a.proposedStoreId}`,a.proposedStoreId,a.proposedLegacyStoreKey,'STORE_ADD','Store ID','',a.proposedStoreId,now,'SOURCE_MASTER_SYNC','SYSTEM']]);return{ok:true,operation:'ADD',storeId:a.proposedStoreId,storeName:text(sourceRow['Current Store Name']),legacyStoreKey:a.proposedLegacyStoreKey,poeFolderId:storeFolder.id,poeFolderCreated:storeFolder.created,active:true};}

async function selfTest(){const defs=new Map([['SET 1',{Set:'SET 1',Flyers:'150',Posters:'30','Price Puppy':'2','Price Adult':'2','Price Small Breed':'2','Price Generic':'1','Available Here':'6',Banner:'4',Shirt:'6',Wobbler:'0','Shelf Strip':'0'}]]),live=[{'Store ID':'SMF-0001','Store Key':'Team 1|Sept 2|1|Alpha','Store Name':'Alpha','Assigned Team':'Team 1',Day:'Sept 2','Stop No.':'1','Store Category':'Feeds Store',Address:'A',Barangay:'B',Area:'QC','Material Allocation':'SET 1',Active:'TRUE'}],base={__row:2,'Store ID':'SMF-0001','Current Store Name':'Alpha New','Assigned Team':'Team 1','Route Day':'DAY 1','Route Stop':'1','Scheduled Deployment Date':'2026-09-02','Scheduled Stop':'1','Store Category':'Feeds Store','Street Address / Location':'A','Barangay / District':'B','City / Area':'QC','Material Allocation':'SET 1','Active':'TRUE','Legacy Store Key':live[0]['Store Key']};let p=plan([base],live,defs);if(p.actions[0].action!=='RENAME_READY')throw new Error('rename test failed');p=plan([{...base,'Assigned Team':'Team 2'}],live,defs);if(p.actions[0].action!=='BLOCKED')throw new Error('forbidden edit test failed');const add={...base,__row:3,'Store ID':'','Legacy Store Key':'','Current Store Name':'Gamma','Scheduled Stop':'2','Route Stop':'2','Street Address / Location':'C'};p=plan([add],live,defs);if(p.actions[0].action!=='ADD_READY')throw new Error('add test failed');const partial={...live[0],'Store ID':'SMF-0002','Store Key':'Team 1|Sept 2|2|Gamma','Store Name':'Gamma','Stop No.':'2',Address:'C'};p=plan([add],[...live,partial],defs);if(p.actions[0].action!=='ADD_RECOVER_READY')throw new Error('recovery test failed');return{ok:true,tests:['rename allowlist','forbidden edit block','new-store candidate','partial-add recovery','material derivation','immutable legacy key','no delete path']};}

if(process.argv.includes('--self-test')){console.log(JSON.stringify(await selfTest(),null,2));process.exit(0);}
const tok=await token();const[sourceValues,liveValues,materialValues]=await Promise.all([getRange(tok,SOURCE_SHEET_ID,`'${IDENTITY_SHEET}'!A1:R1000`),getRange(tok,INTERNAL_SHEET_ID,'V4_STORES!A1:S1000'),getRange(tok,SOURCE_SHEET_ID,`'${MATERIAL_SHEET}'!A1:M100`)]);const sourceHeader=sourceValues[0]?.map(text)||[],liveHeader=liveValues[0]?.map(text)||[];for(const h of SOURCE_HEADERS)if(!sourceHeader.includes(h))throw new Error(`Source identity schema missing: ${h}`);for(const h of V4_HEADERS)if(!liveHeader.includes(h))throw new Error(`V4_STORES schema missing: ${h}`);const defs=materialDefinitions(materialValues),source=rows(sourceValues),live=rows(liveValues),p=plan(source,live,defs);const report={generatedAt:isoNow(),mode:MODE,counts:{sourceRows:source.length,liveStores:live.length,renameReady:p.actions.filter(x=>x.action==='RENAME_READY').length,addReady:p.actions.filter(x=>x.action==='ADD_READY').length,addRecoverReady:p.actions.filter(x=>x.action==='ADD_RECOVER_READY').length,blocked:p.actions.filter(x=>x.action==='BLOCKED').length,unchanged:p.actions.filter(x=>x.action==='UNCHANGED').length},fingerprint:p.fingerprint,hardBlocks:p.hardBlocks,actions:p.actions,protected:['V4_POE','V4_PHOTOS','Drive files','upload/runtime','existing Legacy Store Keys'],deletePath:false};fs.mkdirSync(OUT_DIR,{recursive:true});fs.writeFileSync(`${OUT_DIR}/source-master-sync-plan.json`,JSON.stringify(report,null,2));if(MODE==='PLAN')console.log(JSON.stringify(report,null,2));else if(MODE==='APPLY_RENAME')console.log(JSON.stringify(await applyRename(tok,requireApply(p,'RENAME'),sourceValues,liveValues),null,2));else if(MODE==='APPLY_ADD')console.log(JSON.stringify(await applyAdd(tok,requireApply(p,'ADD'),sourceValues,liveValues,defs),null,2));else throw new Error(`Unsupported SOURCE_MASTER_SYNC_MODE: ${MODE}`);
